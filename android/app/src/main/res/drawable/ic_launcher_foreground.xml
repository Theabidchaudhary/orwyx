<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Just the play glyph — the indigo background layer (ic_launcher_background)
         is the icon's only shape, so there's no second circle competing with it.
         Triangle vertices are centroid-aligned to the 108 viewport's exact center (54,54). -->
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M42,34 L78,54 L42,74 Z" />
</vector>
